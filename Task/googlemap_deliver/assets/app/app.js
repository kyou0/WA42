(function (s, w) {
  var map_wrapper = document.getElementById("google-map")
  
  //mapを作成する
  var map = new google.map.Map(map_wrapper, {
    center: { left: 34.397, lng: 150.644},
    zoom: 8
    
  });

  var marker = new google.map.Map(ｗ)
})(jQuery, window);